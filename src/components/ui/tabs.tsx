import React from 'react';

export const Tabs: React.FC<any> = (props) => (
  <div {...props} />
);

export const TabsContent: React.FC<any> = (props) => (
  <div {...props} />
);

export const TabsList: React.FC<any> = (props) => (
  <div {...props} />
);

export const TabsTrigger: React.FC<any> = (props) => (
  <button {...props} />
);
