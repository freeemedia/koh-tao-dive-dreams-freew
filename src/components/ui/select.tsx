import React from 'react';

export const Select: React.FC<any> = (props) => (
  <select {...props} />
);

export const SelectContent: React.FC<any> = (props) => (
  <div {...props} />
);

export const SelectItem: React.FC<any> = (props) => (
  <option {...props} />
);

export const SelectTrigger: React.FC<any> = (props) => (
  <button {...props} />
);

export const SelectValue: React.FC<any> = (props) => (
  <span {...props} />
);
