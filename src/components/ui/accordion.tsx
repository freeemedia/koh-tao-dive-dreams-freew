import React from 'react';

export const Accordion: React.FC<any> = (props) => (
  <div {...props} />
);

export const AccordionContent: React.FC<any> = (props) => (
  <div {...props} />
);

export const AccordionItem: React.FC<any> = (props) => (
  <div {...props} />
);

export const AccordionTrigger: React.FC<any> = (props) => (
  <button {...props} />
);
