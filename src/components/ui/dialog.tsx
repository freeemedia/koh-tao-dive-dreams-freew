import React from 'react';

export const Dialog: React.FC<any> = (props) => (
  <div {...props} />
);

export const DialogContent: React.FC<any> = (props) => (
  <div {...props} />
);

export const DialogDescription: React.FC<any> = (props) => (
  <div {...props} />
);

export const DialogHeader: React.FC<any> = (props) => (
  <div {...props} />
);

export const DialogTitle: React.FC<any> = (props) => (
  <div {...props} />
);

export const DialogTrigger: React.FC<any> = (props) => (
  <button {...props} />
);
